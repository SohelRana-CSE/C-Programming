#include<stdio.h>
int main()
{
    int a,b,sum,minus,into,divided;
    scanf("%d%d",&a,&b);
    sum=a+b;
    printf("The sum is = %d\n",sum);

    minus=a-b;
    printf("The minus is = %d\n",minus);

    into=a*b;
    printf("The into is = %d\n",into);

    divided=a/b;
    printf("The divided is = %d",divided);


    return 0;
}
